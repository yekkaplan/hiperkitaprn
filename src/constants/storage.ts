export enum StorageKeys {
  // Auth related
  TOKEN = 'token',
  USER = 'user',
  APP_TOKEN = 'appToken',
  
  // Theme related
  THEME = 'theme',
  
  // Language related
  LANGUAGE = 'language',
  
  // User preferences
  PREFERENCES = 'preferences',
} 